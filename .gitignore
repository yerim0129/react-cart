# 의존성 패키지 (npm install 하면 자동 생성됨)
node_modules/

# 빌드 결과물 (vite build 하면 자동 생성됨)
dist/

# 환경변수 (API 키, 비밀번호 등 절대 올리면 안 됨)
.env
.env.local
.env.*.local

# 에디터 설정 (개인 설정이라 공유 불필요)
.vscode/
.idea/

# OS 자동 생성 파일
.DS_Store
Thumbs.db

# 로그
*.log
npm-debug.log*

# TypeScript 빌드 캐시
*.tsbuildinfo
